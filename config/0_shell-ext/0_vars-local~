# scripts that call 0_vars-common and -local:
#  load local vars _after_ common vars

# edit these on a per-machine basis! ^^
this_machine='Sandbox Stations W1'

# server file trees no need put .local extension on dirs
[ $(uname -n) == "$srvName" ] && L=''

# -- action directory for experimentation purposes
# -- pick a dir or make up one
# --  default is /home/$USER/tempdir (aliased as '~/tempdir')
# -- the var furthest down gets the value
zdir=~
#zdir=~/tempdir
#zdir=~/sandbox$L/lab_dir
#zdir=~/lab_dir
#idir='some/faraway/dir'

# -- this is for scripts that use the 'cd' command,
# -- so to allow it to hop back
init_dir=~/scripts$L/.sbsys_init   # call this in scripts wherever cd is done
