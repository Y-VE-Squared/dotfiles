# tmux config file

set -g prefix C-Space
unbind-key C-b
bind-key C-Space send-prefix

bind-key R source-file ~/.tmux.conf

set -g mouse on

unbind-key '%'
unbind-key '"'
bind-key '-' split-window -v
bind-key '=' split-window -h




set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'github_username/plugin_name#branch'
# set -g @plugin 'git@github.com:user/plugin'
# set -g @plugin 'git@bitbucket.com:user/plugin'

set -g @plugin 'joshmedeski/tmux-nerd-font-window-name'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-pain-control'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'MunifTanjim/tmux-suspend'
set -g @plugin 'idevtier/tmux-cargo'
set -g @plugin 'jaclu/tmux-menus'
set -g @plugin 'b0o/tmux-autoreload'

# theming using tmux-nova

set -g @plugin 'o0th/tmux-nova'

set -g @nova-nerdfonts true
set -g @nova-nerdfonts-left 
set -g @nova-nerdfonts-right 

set -g @nova-segment-mode "#{?client_prefix,Ω,ω}"
set -g @nova-segment-mode-colors "#50fa7b #282a36"

set -g @nova-segment-whoami "#(whoami)@#h"
set -g @nova-segment-whoami-colors "#40b0f0 #282a36"

set -g @nova-pane "#I#{?pane_in_mode,  #{pane_mode},}  #W"

# set -g @nova-rows 0
set -g @nova-segments-0-left "mode"
set -g @nova-segments-0-right "whoami"

# does nothing, i'm using this as a reminder tag
# set display-panes-color #301070
# window/pane customization


# set -g window-border-format
set -g pane-border-style fg=#404040
set -g pane-active-border-style bg=#303070,fg=#4090ff
set -g pane-border-indicators both
set -g fill-character '🮙'


# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# above comment is a lie, I've just managed to override some settings
set -g pane-border-style fg=#404040
set -g pane-active-border-style bg=#303060,fg=#2080ff
